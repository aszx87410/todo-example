{
  "name": "todo-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "5.0.1",
    "serve": "^11.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:server": "npx serve build -l 3000",
    "build": "react-scripts build",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "start-server-and-test start:server http-get://localhost:3000 cy:run",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cypress": "^3.6.0",
    "start-server-and-test": "^1.10.6"
  }
}
